{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "description": "A AvenueJS registry of modules, workflows, links, API routes, and more.",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the registry. Should start with @ (e.g., @avenuejs)."
        },
        "homepage": {
            "type": "string",
            "description": "The homepage URL of the registry."
        },
        "items": {
            "type": "array",
            "description": "The registry items.",
            "items": {
                "$ref": "https://registry.avenuejs.com/registry-item.json"
            },
            "minItems": 1,
            "uniqueItems": true
        }
    },
    "required": [
        "name",
        "homepage",
        "items"
    ]
}